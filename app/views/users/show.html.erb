<h3>Info for current user</h3>
<%= flash[:message]%>
<h4><%= @user.username %></h4>
<div><%= @user.info %></div>
<div><% @user.image_data %></div>
<div><%= link_to 'Delete',
                 user_path(@user),
                 method: :delete,
                 data: { confirm: 'User will be deleted! Are you sure?' } %></div>
<%#= link_to 'Add Photo', new_user_photo_path(@user.id) %>

  <%# if @user.image_data? %>

    <%#= image_tag @user.image_url %>
    <span> | <%# link_to 'Edit', edit_user_photo_path(@user.id, photo) %></span>
  <%# end %>
<br/>